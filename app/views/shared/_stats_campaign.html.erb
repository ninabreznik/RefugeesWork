<% @campaign ||= current_campaign %>
<div class="stats">
  <a href="<%= coowners_campaign_path(@campaign) %>">
    <strong id="coowners" class="stat">
      <%= @campaign.coowners.count %>
     / 5 </strong> coowners
  </a>
</div>