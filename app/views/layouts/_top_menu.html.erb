<div class="top_menu_bar">
  <a href="<%= root_path %>"><%= image_tag 'logo.png', :class => 'logo' %></a>
  <% if user_signed_in? %>
    <div class="logged_user">
      <%= link_to current_user.email, edit_user_registration_path %>
    </div>
  <% else %>
    <div class="not_logged_user">
  <%= link_to "Register/Login", new_user_registration_path %>
    </div>
  <% end %>
  <div class="info">
    <a href="<%= new_lead_path %>" title="Tell us what you need or what can you offer"><i class="fa fa-pencil-square-o"></i></a>
  </div>
</div>
