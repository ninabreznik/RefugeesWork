
<div class="body_leads_new">
<section class="lead_new_photo">
  <div class="lead_new_explanation">
    <div class="lead_new_explanation_first">
      <i class="fa fa-pencil"></i>         Tell us what you seek or offer.      </div>
    <div class="lead_new_explanation_second">
      <i class="fa fa-files-o"></i>        Others get notified about your post.          </div>
    <div class="lead_new_explanation_third">
      <i class="fa fa-check-square-o"></i> Those who are interested will get in touch with you.  </div>
  </div>
  <div class="lead_new">
    <br>
    <div class="lead_new_instructions">
       The offered tasks should be a win-win situation for everyone.
       No part of the equation should come out of the collaboration feeling used.
       Our focus is social engagement through working together. </div>
    <div class="lead_new_title">
      Create new post!
    </div>
    <%= form_for (@lead) do |l| %>
      <%= render "#{@lead.current_step}_step", :l => l %>
      <div class="lead_new_form_submit">
        <%= l.submit "Back", :name => "back_button" unless @lead.first_step? %>
        <%= l.submit "Next" unless @lead.last_step? %>
        <%= l.submit "Send" if @lead.last_step? %>
      </div>
    <% end %>
  </div>
</section>
  <%= render "layouts/disqus" %>
