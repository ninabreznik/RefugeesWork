 <%= link_to(
  "Prevzemi stranko",
  {
    :controller => 'orders', 
    :action => 'show', 
    :id => @lead.reverse_orders.where(selector_id: current_user.id).first.id
    },
  :html =>
  {
    :method => :get, 
    :remote => true
  }
  ) %>
